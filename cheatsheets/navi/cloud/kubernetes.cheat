% kubernetes
@ common

# Run a Kubernetes pod that can use the curl command for debugging
kubectl run curl --image=radial/busyboxplus:curl -i --tty --rm

# List all supported API resources in Kubernetes, either namespaced or non-namespaced resources
kubectl api-resources --namespaced=<true_or_false> -o wide
