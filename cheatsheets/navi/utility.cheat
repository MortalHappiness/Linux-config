% utility

# Use xclip to copy file content to clipboard
xclip -sel clip < <file>

# Run diff-so-fancy to compare two files
diff -u <file_1> <file_2> | diff-so-fancy

# Run diff-so-fancy to compare two folders
diff -r -u <folder_1> <folder_2> | diff-so-fancy
